@model IEnumerable<Market.Models.SellerProduct>

@{
    ViewBag.Title = "GetAllSP";
}

<h2>GetAllSP</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.SellerId)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.ProductId)
        </th>
        @*<th>
            @Html.DisplayNameFor(model => model.Date)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.IsActive)
        </th>*@
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.SellerId)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.ProductId)
        </td>
        @*<td>
            @Html.DisplayFor(modelItem => item.Date)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.IsActive)
        </td>*@
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.SellerProductId }) |
            @Html.ActionLink("Details", "Details", new { id=item.SellerProductId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.SellerProductId })
        </td>
    </tr>
}

</table>
